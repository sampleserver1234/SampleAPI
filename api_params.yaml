environments:
  - name: dev
    endpoints:
      production:
        url: "https://192.168.1.10:9443/"
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: "https://192.168.1.10:9443/"
          config:
            retryTimeOut: $RETRY
